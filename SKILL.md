---
name: henan-univ-thesis
description: |
  Use for 河南科技大学论文、河科大毕设、本科毕业论文的 DOCX 格式精修、官方模板迁移、老师批注处理、图表与截图插入、引文格式修复、Word/PDF 渲染验收，以及必要时的论文框架或批量结构化内容生成。
---

# 河南科技大学本科毕业论文辅助

本 skill 用于河南科技大学本科毕业设计（论文）的格式审查、DOCX 精修、模板迁移、批注响应、图表截图处理和必要的结构化生成。默认目标是让现有论文符合学校官方示例模板与老师批注要求，而不是重新发明排版规范。

## 优先级

1. 官方示例模板和老师批注优先级最高。
2. 本 skill 与官方模板冲突时，以官方模板、批注和用户当前文档的已验收格式为准。
3. 论文进入精修阶段后，优先直接编辑 `.docx`；只在批量重建结构化内容或用户明确要求时使用生成脚本。
4. 重要修改前先备份，保留已人工修好的内容、图题、分页、目录和样式细节。
5. 不要只看样式名称，要检查 OOXML 中实际字体、字号、加粗、`eastAsia` 中文字体、缩进、行距、页眉页脚和图表嵌入关系。

## 工作入口

- 格式、字体、标题、目录、表格、页眉页脚：先读 `references/docx-refinement-workflow.md` 和 `references/format-samples.md`。
- 官方模板重建或同样设置显示差异明显：读 `references/template-migration.md`。
- 修改导致分页变化或涉及视觉结果：读 `references/render-qa.md`。
- 引文上标、参考文献顺序、密集引用清理：读 `references/citation-rules.md`。
- 图表重绘、软件工程图、DOCX 内嵌图片替换、真实浏览器截图：读 `references/image-and-diagram-workflow.md`。
- 老师批阅版、批注、修订处理：读 `references/teacher-comments-workflow.md`。
- 只需要生成基础论文框架或批量结构化内容时，可使用 `scripts/generate_docx.py`，但生成后仍必须按模板复审。

## 核心流程

1. 明确用户要处理的是整篇生成、现有 DOCX 精修、模板迁移、批注处理、图表截图，还是引用修复。
2. 读取对应 reference；涉及格式时同时读取 `references/format-samples.md`。
3. 修改现有 DOCX 前创建语义化备份，例如 `*.before-citation-refine.docx`。
4. 完成内容或结构修改后，做局部结构审计：样式、run 字体、字号、加粗、缩进、行距、图表编号、引用位置。
5. 导出 PDF 并渲染页面图片，检查目录、标题、正文、图表、分页、页眉页脚、参考文献。
6. 如果渲染发现问题，回到 DOCX/OOXML 修复后重新验收。

## 河南科技大学格式基线

- 章标题：三号黑体 16pt，居中，不加粗。
- 节标题：四号黑体 14pt，顶格，不加粗，段前 0.5 行、段后 0.5 行。
- 子节标题：小四号宋体 12pt，顶格，不加粗，段前 0.5 行、段后 0.5 行。
- 正文：中文小四号宋体 12pt，英文和数字 Times New Roman 12pt，单倍行距，首行缩进 2 字符。
- 表题：五号楷体 10.5pt，居中。
- 表头：五号黑体 10.5pt，加粗。
- 表内文字：五号宋体 10.5pt。
- 规范未明确要求加粗的内容，原则上不加粗。

## 引用和图表底线

- 正文引文标示必须是方括号上标，如 `研究[1]` 中的 `[1]` 为上标。
- 各级标题不得放引用标示。
- 不要出现普通正文 `[1]` 或 `[1][2][3][4]` 这类密集堆叠。
- 参考文献表中的文献必须在正文中被实际引用。
- 图表必须清晰、正式，箭头不能遮挡文字；ER 图应区分实体、属性和联系。
- 替换已有图片时，优先替换 DOCX 内部 `word/media/imageX.png`，以保留尺寸、段落位置、图题和分页。

## 验收要求

程序执行成功不等于论文完成。涉及 DOCX 格式的交付至少说明：

- 修改了哪些内容；
- 备份文件名；
- 结构/字体/引用检查结果；
- PDF 渲染验收是否完成；
- 如果无法导出或渲染，说明原因和剩余风险。
